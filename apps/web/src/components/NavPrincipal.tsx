// Navegação de seções migrada para AppShell (drawer lateral).
// Este arquivo preserva apenas o tipo Secao para compatibilidade de imports.

export type Secao = 'compromissos' | 'equipe' | 'revisao' | 'timeline' | 'links'

export function NavPrincipal() {
  return null
}
